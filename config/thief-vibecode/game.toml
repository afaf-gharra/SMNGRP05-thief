# PRIVATE, LOCAL configuration for THIS peer only.
#
# Nothing the opponent must agree on lives here. Every shared term — board size,
# start cells, movement set, barrier quota, scoring, pheromone model, map area,
# hint length — lives ONLY in the signed `game.json` beside this file, which both
# peers hold byte-identically and verify by SHA-256 before the first move.
#
# Book: Appendix B (JSON = shared and signed; TOML = private and local).
version = "1.00"

[game]
group_id   = "SMNGRP05"
group_name = "SMNGRP05"
# vibecode pre-derived this name and sent it before either side connected. It
# equals what our own derivation produces, and it is pinned anyway: the point of
# the derivation is that neither peer names the match alone, so a name both sides
# published in writing is stronger than one each side recomputed privately.
series_id  = "SMNGRP05-vs-vibecode"
# Pinned as a belt-and-braces, NOT because the derivations disagree -- they do
# not. vibecode hash a FLAT 14-key agreement and our repo game.json is nested,
# so we expected to have to agree the string; in fact our shared-terms overlay
# already flattens to exactly those 14 keys, our canonical line is byte-identical
# to theirs, and derive_game_ids() reproduces f3926768-... natively from this
# config. Verified, not assumed. The pin therefore guards against future drift in
# our own terms rather than papering over a live mismatch, and if it ever stops
# equalling the derived value that is a signal worth stopping for.
# Their case test also reproduces: lowercasing us yields 0be6db0d-..., a
# different match. The byte string "SMNGRP05" is load-bearing.
series_uid = "f3926768-06be-e93c-6cba-bd6ebf142059"  # AGREED IN WRITING with vibecode, not derived.
members    = ["Afaf Gharra", "Reem Awawdy"]
counted_games_played = 2  # completed COUNTED series: GRP00001 (saedshki), uoh-ay26.
# declaration is exchanged with opponents and archived in their repos, and the
# lecturer already holds the ID numbers from the submission form.
repos      = { cop = "https://github.com/afaf-gharra/SMNGRP05-police", thief = "https://github.com/afaf-gharra/SMNGRP05-thief" }
# Public tunnel URLs announced to the opponent in the handshake (ngrok/Localtonet).
# Until you open a tunnel these stay on loopback, which is fine for local testing.
mcp_servers = { cop = "https://f0eb-147-236-163-154.ngrok-free.app/mcp", thief = "https://f0eb-147-236-163-154.ngrok-free.app/mcp" }

[network]
host                   = "0.0.0.0"                     # bind wide so a tunnel can reach us
my_port                = 8801                          # MY MCP server port
opponent_url           = "http://62.56.220.143:61223/mcp"  # fallback; see opponent_urls
# vibecode run two SEPARATE OS processes, one per role, on a router-forwarded
# static IP -- two ports, not two hostnames. Their cop answers on 61224 (dial it
# when we are thief) and their thief on 61223 (dial it when we are cop). Roles
# alternate every sub-game, so a single fixed URL would spend half the series
# talking to the peer that is not playing. Their forward DROPS rather than
# refusing when nothing is behind it, so a probe before they start times out
# silently and proves nothing -- do not read it as unreachable.
opponent_urls = { cop = "http://62.56.220.143:61224/mcp", thief = "http://62.56.220.143:61223/mcp" }
turn_timeout_seconds   = 180                           # DeadlineTracker: opponent silence budget
poll_interval_seconds  = 0.5
connect_timeout_seconds = 300
call_timeout_seconds    = 10   # strictly under the signed response_timeout_sec of 30  # W010: their cop was still 502 well after kickoff,
# and both restarts of ours only reset a five-minute clock without changing that.
# Wait half an hour for the opponent to appear rather than scoring sub-game 1 a
# technical loss and forcing yet another coordinated restart. Private, local, not
# a signed term. The per-turn budget stays at 180s, so a peer that connects and
# then dies is still caught quickly -- this only buys patience before the series
# has begun, which is exactly where the waiting has actually been.
retry_interval_seconds = 1.0
audit_send_timeout_seconds = 10
watchdog_timeout_seconds = 300                         # Watchdog: main-loop freeze budget

[belief]
smell_trust_weight = 4.0    # how sharply a scent reading concentrates the belief mass
hint_trust_initial = 0.5    # prior P(opponent's hint is truthful)
hint_trust_floor   = 0.05   # never fully trust or fully dismiss a talker
hint_trust_ceiling = 0.95

[strategy]
# The graded seam. Point either key at a BrainBase subclass "package.module:Class".
police_class = "p2p_chase.strategy.police_brain:ArchitectPolice"
thief_class  = "p2p_chase.strategy.safe_thief:SafeThief"

[strategy.police]
capture_confidence = 0.45   # belief mass on one adjacent cell before we claim a capture.
#                             Raised from 0.18 after a live loss: at that bar the officer
#                             claimed ten times in one sub-game, caught nothing, announced
#                             its own cell each time, and oscillated chasing the belief peak.
wall_threshold     = 3.0    # structural value a wall must clear before we give up a turn to build (tuned: docs/RESEARCH-REPORT.md)
barrier_reserve    = 3      # walls held back for the endgame squeeze
anchor_bonus       = 1.2    # per side already touching a wall or the board edge
cut_bonus          = 6.0    # sealing a cut vertex severs a corridor outright
occupancy_bonus    = 12.0   # sealing a cell the thief is standing on IS a capture
own_region_floor   = 0.25   # never wall ourselves into less than this share of the board
pressure_weight    = 1.0    # weight on expected walking distance to the thief
containment_weight = 0.5    # weight on shrinking the thief's reachable region
coverage_weight    = 1.2    # weight on belief mass within one step of us
centrality_weight  = 0.2    # mild pull toward the centre of mass on quiet turns

[strategy.thief]
region_weight    = 1.0      # weight on "how much room do I still have"
mobility_weight  = 3.0      # weight on "how many cells can I reach in three moves"
#                           A corner reports full region size and two exits, so
#                           without this term nothing sees the trap closing.
distance_weight  = 0.35     # weight on "how far from the believed officer".
#                           Lowered from 0.6: raw distance is what walks a thief
#                           into the far corner, which is exactly how uoh-ay26
#                           took all three of our thief sub-games at step 12.
novelty_weight   = 0.15     # mild preference for unvisited cells
deadend_penalty  = 0.8      # a cell with one exit is one barrier from losing
contact_penalty  = 1.5      # never volunteer to stand next to the officer
exposure_weight  = 0.4      # weight on belief mass within two steps

[trash_talk]
# HOW the natural-language hint is written. The MOVE is ALWAYS pure Python and is
# never delegated to a model (book ch.6, mandatory rule 25).
provider      = "template"   # template (0 tokens, default) | claude_api | ollama | claude_cli
model         = "claude-haiku-4-5-20251001"
every_n_steps = 1
lie_rate      = 0.45         # target fraction of hints that are deliberate misdirection

[llm]
model                  = "template-zero-token"
step_deadline_seconds  = 20   # hard cap on hint generation; miss => template fallback
timeout_seconds        = 60

[email]
enabled   = true
recipient = "agentsorch@gmail.com, afafgharra000@gmail.com"   # FRIENDLY: mutual exchange
# FRIENDLY: mutual exchange, which vibecode ask for and are right to. A report
# each side files only to itself cannot be checked by the other, and the whole
# point of the mutual audit is that neither side takes the other's result on
# trust. Ourselves kept on the line so we hold a copy. The lecturer is involved
# in NOTHING until a counted series; mailing a warm-up there would be a false
# declaration under rules 37-38. Flip to rmisegal+uoh26finalgame@gmail.com ALONE
# for the counted, by hand, before the match starts -- never mid-run.
sender    = "me"

[league]
# Six-sub-game friendly whose purpose is byte-reconciliation in both directions
# before anything counts.
series_consensus = false   # vibecode do not exchange it; never send an unknown
#                            key to a peer that refuses unknown keys.
counted       = false   # FRIENDLY. Set true ONLY for the counted series.
first_meeting = true    # no counted series with vibecode yet
# Declared physics. subtractive_chebyshev_v1, the kit CORE model: vibecode sent
# this exact hash and it already equalled ours, unchanged, from before their
# message. They treat it as the one hard gate -- declared-and-different aborts
# the series before move one, while omission refuses nothing.
# info_mode_sha256 is ours and they did not mention one; they accept supersets,
# but it is declared to them in writing so a differing value surfaces before T
# rather than at step 0.
scent_model_sha256 = "81ebee59640e80eae8ca9ee5f86abd26e7edf5cdbb27d15925cb6ee45ca6ddf4"
info_mode_sha256   = "020947daeeb3f73494af9b04201326791742c7184085456e3517d21981ee1202"

[play]
seed               = 19470529
step_speed_seconds = 0.35

[gui]
cell_px = 56

[paths]
logs_dir  = "logs"
state_dir = "state"
